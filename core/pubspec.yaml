name: drakkar_core
description: Shared models and validation for Drakkar (used by the API server and the Flutter client).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
